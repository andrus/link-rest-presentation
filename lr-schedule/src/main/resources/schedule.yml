jetty:
  connectors:
    - port: 8082

jdbc:
  schedule:
    url: "jdbc:mysql://127.0.0.1:3306/schedule?connectTimeout=0&autoReconnect=true"
    initialSize: 1
    username: root
    password: root

log:
  loggers:
    io.bootique.jersey.client:
      level: DEBUG